- name: Hello World Sample
  hosts: all
  tasks:
    - name: Hello Message
      debug:
#       msg: "Hello LoicA!"
        msg: "{{ ansible_facts.architecture }}"
    - name: Print all available facts
      debug:
        var: ansible_facts
        
